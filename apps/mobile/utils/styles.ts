// export const primary = '#007AFF';
export const primary = '#107927ff';

// The neutrals every screen was inventing for itself. Before this there were
// ten different grays in play (#999, #333, #6c757d, #ccc, 'grey'…), which is
// why no two screens quite matched. Reach for these instead of a fresh hex.
export const ink = '#22241f';        // body copy, titles
export const inkMuted = '#6c716a';   // supporting copy, section labels
export const inkFaint = '#9aa096';   // counts, metadata, placeholders
export const surface = '#f5f7f3';    // soft cards sitting on white
export const hairline = '#e4e7e0';   // dividers, rules, resting borders

// primary at 10% — the tint behind step numbers, tags and other quiet accents.
// Written out rather than composed, since `primary` already carries an alpha.
export const accentSoft = 'rgba(16, 121, 39, 0.10)';
